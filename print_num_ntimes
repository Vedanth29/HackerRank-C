#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    int n,i,j;
    printf("enter a number");
    scanf("%d", &n);
  	for(i = 0;i<=n;i++)
  	{
  	    for(j=0;j<i;j++)
  	
      printf("%d\n",i);
  	}
    return 0;
}
